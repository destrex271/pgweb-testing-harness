{
    "defaults": {
        "reporters": [
            "cli", 
            "pa11y-reporter-html",
            "./my-local-reporter.js"
        ],
        "chromeLaunchConfig": {
            "args": ["--no-sandbox --headless --disable-gpu"]
        }
    }
}
